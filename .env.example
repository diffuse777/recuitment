# Optional for LOCAL frontend only (use .env.local — never set this on Vercel)
# VITE_API_URL=http://localhost:5000

# On Vercel production:
# 1. Leave VITE_API_URL UNSET (or delete it if it points to localhost)
# 2. Redeploy after removing it
# 3. Set these SERVER env vars in Vercel → Settings → Environment Variables:

# MONGO_URI=mongodb+srv://...
# JWT_SECRET=supersecretjwtkey_replace_me_in_production
# ADMIN_USERNAME=cybernerdsXowsap
# ADMIN_PASSWORD=recuitment@0007
# FRONTEND_URL=https://owaspxcybernerds.xyz
